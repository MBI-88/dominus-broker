package queue

import (
	"context"
	"dominus-project/internal/domain/entities"
)

func (c *queue) Consumer(ctx context.Context) (*entities.Queue, error) {
	// Checking if memory is empty, if this is true, fill it after answering 
	return  nil, nil
}