apiVersion: apps/v1
kind: Deployment
metadata:
  name: dominus
  namespace: default
  labels:
    app: dominus
spec:
  selector:
    matchLabels:
      app: dominus
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: dominus
    spec:
      containers:
        - name: dominus
          image: mbi8807/dominus:latest # public repo only test
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 100m
              memory: 100Mi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - "wget -qO- --header=API_TOKEN $API_TOKEN http://localhost:8000/health"
            initialDelaySeconds: 60
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
            periodSeconds: 60

          env:
            - name: API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: API_TOKEN

            - name: REST_PORT
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: REST_PORT

            - name: GRPC_PORT
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: GRPC_PORT

            - name: SSL_CERT
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: SSL_CERT

            - name: SSL_CA
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: SSL_CA

            - name: KEY_FILE
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: KEY_FILE

            - name: LOGS
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: LOGS

            - name: WRITE_TIMEOUT
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: WRITE_TIMEOUT

            - name: READ_TIMEOUT
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: READ_TIMEOUT

            - name: IDLE_TIMOUTLOGS
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: IDLE_TIMOUT

            - name: MAX_CONNS_PER_IP
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: MAX_CONNS_PER_IP

            - name: MAX_REQUEST_PER_CONN
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: MAX_REQUEST_PER_CONN

            - name: MAX_REQUEST_BODY_SIZE
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: MAX_REQUEST_BODY_SIZE

            - name: REDUCE_MEMORY_USAGE
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: REDUCE_MEMORY_USAGE

            - name: DISABLE_HEADER_NAMES_NORMALIZING
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: DISABLE_HEADER_NAMES_NORMALIZING

            - name: SLEEP_WHEN_CONCURRENCY_LIMIT_EXCEDEED
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: SLEEP_WHEN_CONCURRENCY_LIMIT_EXCEDEED

            - name: NO_DEFAULT_DATE
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: NO_DEFAULT_DATE

            - name: KEEP_HIJACKED_CONNS
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: KEEP_HIJACKED_CONNS

            - name: CLOSE_ON_SHUTDOWN
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: CLOSE_ON_SHUTDOWN

            - name: STREAM_REQUEST_BODY
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: STREAM_REQUEST_BODY

            - name: DSN
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: DSN

            - name: CIDR
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: CIDR

            - name: DATABASE
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: DATABASE

            - name: COLLECTIONS
              valueFrom:
                secretKeyRef:
                  name: dominus-config
                  key: COLLECTIONS

          ports:
            - containerPort: 8000
              name: rest
            - containerPort: 5000
              name: grpc

          volumeMounts:
            - name: dominus-certs
              mountPath: /etc/dominus/certs
              readOnly: true
            - name: dominus-logs
              mountPath: /var/dominus/logs
              readOnly: true

      volumes:
        - name: dominus-certs
          persistentVolumeClaim:
            claimName: dominus-certs-pvc
        - name: dominus-logs
          persistentVolumeClaim:
            claimName: dominus-logs-pvc

      restartPolicy: Always


---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dominus-certs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Ki

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dominus-logs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi 


---
apiVersion: v1
kind: Service
metadata:
  name: dominus-rest
  namespace: default
spec:
  selector:
    app: dominus
  sessionAffinity: None
  type: ClusterIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
    - name: rest
      protocol: TCP
      port: 8000
      targetPort: 8000 

--- 
apiVersion: v1
kind: Service
metadata:
  name: dominus-grpc
  namespace: default
spec:
  selector:
    app: dominus
  type: LoadBalancer
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: grpc
    protocol: TCP
    port: 5000
    targetPort: 5000
